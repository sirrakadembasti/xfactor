export const CHOICES = {\n  rock: {\n    id: 'rock',\n    name: 'Taş',\n    icon: '🪨',\n    color: 'from-amber-500 to-red-600',\n    borderColor: 'border-amber-500',\n    shadowColor: 'shadow-amber-500/30',\n    beats: {\n      scissors: 'Taş makası kırar',\n      lizard: 'Taş kertenkeleyi ezer'\n    }\n  },\n  paper: {\n    id: 'paper',\n    name: 'Kağıt',\n    icon: '📄',\n    color: 'from-blue-400 to-indigo-600',\n    borderColor: 'border-blue-400',\n    shadowColor: 'shadow-blue-400/30',\n    beats: {\n      rock: 'Kağıt taşı sarar',\n      spock: 'Kağıt Spock\\'ı çürütür'\n    }\n  },\n  scissors: {\n    id: 'scissors',\n    name: 'Makas',\n    icon: '✂️',\n    color: 'from-rose-500 to-pink-600',\n    borderColor: 'border-rose-500',\n    shadowColor: 'shadow-rose-500/30',\n    beats: {\n      paper: 'Makas kağıdı keser',\n      lizard: 'Makas kertenkelenin kafasını koparır'\n    }\n  },\n  lizard: {\n    id: 'lizard',\n    name: 'Kertenkele',\n    icon: '🦎',\n    color: 'from-emerald-400 to-teal-600',\n    borderColor: 'border-emerald-400',\n    shadowColor: 'shadow-emerald-400/30',\n    beats: {\n      spock: 'Kertenkele Spock\\'ı zehirler',\n      paper: 'Kertenkele kağıdı yer'\n    }\n  },\n  spock: {\n    id: 'spock',\n    name: 'Spock',\n    icon: '🖖',\n    color: 'from-purple-500 to-cyan-600',\n    borderColor: 'border-purple-500',\n    shadowColor: 'shadow-purple-500/30',\n    beats: {\n      scissors: 'Spock makası parçalar',\n      rock: 'Spock taşı buharlaştırır'\n    }\n  }\n};\n\nexport const MODE_CHOICES = {\n  classic: ['rock', 'paper', 'scissors'],\n  extended: ['rock', 'paper', 'scissors', 'lizard', 'spock']\n};\n\nexport function determineWinner(playerChoice, cpuChoice) {\n  if (playerChoice === cpuChoice) {\n    return { result: 'tie', reason: 'Aynı seçimi yaptınız!' };\n  }\n\n  const playerRule = CHOICES[playerChoice];\n  if (playerRule && playerRule.beats[cpuChoice]) {\n    return {\n      result: 'win',\n      reason: playerRule.beats[cpuChoice]\n    };\n  }\n\n  const cpuRule = CHOICES[cpuChoice];\n  return {\n    result: 'lose',\n    reason: cpuRule.beats[playerChoice] || 'Rakip kazandı!'\n  };\n}\n\nexport function getCpuChoice(availableChoices, difficulty, history = []) {\n  if (difficulty === 'easy' || history.length < 2) {\n    const randomIndex = Math.floor(Math.random() * availableChoices.length);\n    return availableChoices[randomIndex];\n  }\n\n  // Oyuncunun en çok kullandığı hamleyi bul\n  const counts = {};
  history.forEach(h => {\n    counts[h.player] = (counts[h.player] || 0) + 1;\n  });\n\n  let mostFrequentPlayerChoice = availableChoices[0];\n  let maxCount = -1;\n  Object.keys(counts).forEach(choice => {\n    if (availableChoices.includes(choice) && counts[choice] > maxCount) {\n      maxCount = counts[choice];\n      mostFrequentPlayerChoice = choice;\n    }\n  });\n\n  // Zor seviye: Oyuncunun en çok seçtiği seçeneği yenen hamleyi seç\n  if (difficulty === 'hard') {\n    const winningChoices = availableChoices.filter(c => {\n      return CHOICES[c].beats[mostFrequentPlayerChoice];\n    });\n    if (winningChoices.length > 0) {\n      return winningChoices[Math.floor(Math.random() * winningChoices.length)];\n    }\n  }\n\n  // Orta seviye %60 oranında stratejik, %40 rastgele\n  if (difficulty === 'medium' && Math.random() < 0.6) {\n    const winningChoices = availableChoices.filter(c => CHOICES[c].beats[mostFrequentPlayerChoice]);\n    if (winningChoices.length > 0) {\n      return winningChoices[Math.floor(Math.random() * winningChoices.length)];\n    }\n  }\n\n  const randomIndex = Math.floor(Math.random() * availableChoices.length);\n  return availableChoices[randomIndex];\n}\n