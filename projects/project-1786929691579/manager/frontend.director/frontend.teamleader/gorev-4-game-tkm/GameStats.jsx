import React from 'react';\nimport { CHOICES } from './utils/gameLogic';\n\nexport default function GameStats({ stats, history, onResetStats }) {\n  const total = stats.wins + stats.losses + stats.ties;\n  const winRate = total > 0 ? Math.round((stats.wins / total) * 100) : 0;\n\n  return (\n    <div className=\"bg-slate-900/80 backdrop-blur-md border border-slate-800 rounded-2xl p-5 text-slate-200 shadow-xl space-y-5\">\n      <div className=\"flex justify-between items-center border-b border-slate-800 pb-3\">\n        <h3 className=\"text-lg font-bold text-slate-100 flex items-center gap-2\">\n          📊 Detaylı İstatistikler\n        </h3>\n        <button\n          onClick={onResetStats}\n          className=\"text-xs text-rose-400 hover:text-rose-300 hover:bg-rose-950/40 px-2.5 py-1 rounded-lg transition-colors border border-rose-900/50\"\n        >\n          Sıfırla\n        </button>\n      </div>\n\n      {/* Özet Kartları */}\n      <div className=\"grid grid-cols-2 sm:grid-cols-4 gap-3 text-center\">\n        <div className=\"bg-slate-800/60 border border-slate-700/50 p-3 rounded-xl\">\n          <div className=\"text-xs text-slate-400 font-medium mb-1\">Kazanma Oranı</div>\n          <div className=\"text-2xl font-black text-emerald-400\">%{winRate}</div>\n        </div>\n        <div className=\"bg-slate-800/60 border border-slate-700/50 p-3 rounded-xl\">\n          <div className=\"text-xs text-slate-400 font-medium mb-1\">Mevcut Seri</div>\n          <div className=\"text-2xl font-black text-amber-400\">🔥 {stats.streak}</div>\n        </div>\n        <div className=\"bg-slate-800/60 border border-slate-700/50 p-3 rounded-xl\">\n          <div className=\"text-xs text-slate-400 font-medium mb-1\">En Yüksek Seri</div>\n          <div className=\"text-2xl font-black text-cyan-400\">🏆 {stats.bestStreak}</div>\n        </div>\n        <div className=\"bg-slate-800/60 border border-slate-700/50 p-3 rounded-xl\">\n          <div className=\"text-xs text-slate-400 font-medium mb-1\">Toplam Maç</div>\n          <div className=\"text-2xl font-black text-slate-200\">{total}</div>\n        </div>\n      </div>\n\n      {/* Son Hamle Geçmişi */}\n      {history.length > 0 && (\n        <div>\n          <h4 className=\"text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2\">\n            Son Hamleler\n          </h4>\n          <div className=\"space-y-2 max-h-48 overflow-y-auto pr-1\">\n            {history.slice(0, 10).map((item, idx) => (\n              <div\n                key={idx}\n                className=\"flex justify-between items-center bg-slate-800/40 border border-slate-700/30 p-2.5 rounded-xl text-xs\"\n              >\n                <div className=\"flex items-center gap-2\">\n                  <span className=\"text-base\">{CHOICES[item.player]?.icon}</span>\n                  <span className=\"text-slate-300 font-medium\">vs</span>\n                  <span className=\"text-base\">{CHOICES[item.cpu]?.icon}</span>\n                </div>\n                <span className={`font-bold px-2 py-0.5 rounded-full text-[10px] uppercase ${`\n                  ${item.result === 'win' ? 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30' : ''}\n                  ${item.result === 'lose' ? 'bg-rose-500/20 text-rose-400 border border-rose-500/30' : ''}\n                  ${item.result === 'tie' ? 'bg-amber-500/20 text-amber-400 border border-amber-500/30' : ''}\n                `}`}>\n                  {item.result === 'win' ? 'Galibiyet' : item.result === 'lose' ? 'Mağlubiyet' : 'Berabere'}\n                </span>
              </div>\n            ))}\n          </div>\n        </div>\n      )}\n    </div>\n  );\n}\n