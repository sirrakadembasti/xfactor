import React from 'react';\nimport { CHOICES } from './utils/gameLogic';\n\nexport default function RulesModal({ isOpen, onClose, mode }) {
  if (!isOpen) return null;\n\n  return (\n    <div className=\"fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-950/80 backdrop-blur-sm animate-fade-in\">\n      <div className=\"bg-slate-900 border border-slate-800 rounded-2xl max-w-lg w-full p-6 text-slate-100 shadow-2xl relative overflow-hidden\">\n        {/* Accent Glow */}\n        <div className=\"absolute -top-12 -right-12 w-32 h-32 bg-cyan-500/10 rounded-full blur-2xl pointer-events-none\" />\n        \n        <div className=\"flex justify-between items-center mb-6 border-b border-slate-800 pb-4\">\n          <h2 className=\"text-xl font-bold bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent\">\n            Nasıl Oynanır? ({mode === 'extended' ? 'Taş-Kağıt-Makas-Kertenkele-Spock' : 'Klasik'})\n          </h2>\n          <button\n            onClick={onClose}\n            className=\"text-slate-400 hover:text-white transition-colors text-2xl font-bold leading-none px-2 py-1 rounded-lg hover:bg-slate-800\"\n          >\n            ✕\n          </button>\n        </div>\n\n        <div className=\"space-y-4 max-h-[65vh] overflow-y-auto pr-1 text-sm text-slate-300\">\n          <p className=\"text-slate-400\">\n            Seçimini yap ve bilgisayarla kapış! Her kazandığın turda puan ve seri kazanırsın.\n          </p>\n\n          <div className=\"grid gap-3\">\n            {Object.values(CHOICES)\n              .filter(choice => mode === 'extended' || ['rock', 'paper', 'scissors'].includes(choice.id))\n              .map(item => (\n                <div key={item.id} className=\"p-3 bg-slate-800/60 rounded-xl border border-slate-700/50 flex flex-col gap-1\">\n                  <div className=\"flex items-center gap-2 font-semibold text-slate-100\">\n                    <span className=\"text-2xl\">{item.icon}</span>\n                    <span className=\"capitalize text-cyan-300\">{item.name}</span>\n                  </div>\n                  <ul className=\"text-xs text-slate-400 space-y-1 mt-1 pl-2\">\n                    {Object.entries(item.beats).map(([target, reason]) => (\n                      <li key={target} className=\"flex items-center gap-1.5\">\n                        <span className=\"text-emerald-400 font-bold\">✓</span> {reason}\n                      </li>\n                    ))}\n                  </ul>\n                </div>\n              ))}\n          </div>\n\n          {mode === 'extended' && (\n            <div className=\"mt-4 p-3 bg-cyan-950/40 border border-cyan-800/40 rounded-xl text-xs text-cyan-200\">\n              💡 <strong>Spock & Kertenkele İpucu:</strong> Big Bang Theory dizisinden popülerleşen bu versiyonda olasılıklar daha dengelidir (her seçim tam 2 seçeneği yener, 2 seçeneğe yenilir).\n            </div>\n          )}\n        </div>\n\n        <div className=\"mt-6 text-right\">\n          <button\n            onClick={onClose}\n            className=\"px-5 py-2.5 bg-gradient-to-r from-cyan-500 to-blue-600 hover:from-cyan-400 hover:to-blue-500 text-white font-medium rounded-xl shadow-lg transition-all transform active:scale-95\"\n          >\n            Anladım, Oyuna Dön\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n}\n