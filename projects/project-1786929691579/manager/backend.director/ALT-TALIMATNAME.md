{
  "domain": "backend",
  "altTalimatname": "# Backend Alt-Talimatnamesi (kücük-oyunlar)\n\n## 1. Genel Bakış ve Mimari Yaklaşım\nBu alt-talimatname, 'kücük-oyunlar' projesinin veritabanı mimarisini, veri modellerini, API rotalarını (Next.js Route Handlers / Server Actions) ve iş mantığını tanımlar. Backend katmanı %100 Türkçe içerik desteği, SQLite + Prisma ORM veritabanı ve performanslı REST uç noktaları üzerine inşa edilecektir.\n\n---\n\n## 2. Teknolojik Yapı ve Kütüphaneler\n- **ORM:** Prisma ORM\n- **Veritabanı:** SQLite\n- **Doğrulama (Validation):** Zod\n- **API Mimarisi:** Next.js App Router (Route Handlers `/app/api/...` ve Server Actions)\n\n---\n\n## 3. Veritabanı Şeması (Prisma Models)\n\n### `Score` Modeli (Liderlik Tablosu)\n```prisma\nmodel Score {\n  id          String   @id @default(uuid())\n  gameType    String   // MEMORY, ROCK_PAPER_SCISSORS, TIC_TAC_TOE, SNAKE, WORDLE\n  playerName  String\n  score       Int\n  durationSec Int?     // Oyunda geçen süre (saniye)\n  createdAt   DateTime @default(now())\n\n  @@index([gameType, score])\n}\n```\n\n### `Word` Modeli (Türkçe Kelime Tahmin Oyunu)\n```prisma\nmodel Word {\n  id        String   @id @default(uuid())\n  word      String   @unique // 5 harfli Türkçe kelime (küçük harf / UTF-8)\n  length    Int      @default(5)\n  createdAt DateTime @default(now())\n}\n```\n\n---\n\n## 4. API Endpoints ve Servis Şartnamesi\n\n### 4.1. Liderlik Tablosu Servisleri (`/api/leaderboard`)\n- **`GET /api/leaderboard?game={gameType}&limit={10}`**\n  - Belirtilen oyun türüne ait en yüksek skorları döndürür.\n  - Yılan ve Hafıza oyunu için skor/süre sıralaması yapılır.\n- **`POST /api/leaderboard`**\n  - Body: `{ gameType: string, playerName: string, score: number, durationSec?: number }`\n  - Zod şeması ile veri doğrulandıktan sonra veritabanına eklenir.\n\n### 4.2. Kelime Oyunu Servisleri (`/api/words`)\n- **`GET /api/words/random`**\n  - Kelime havuzundan rastgele 5 harfli Türkçe bir kelime seçer.\n- **`POST /api/words/validate`**\n  - Body: `{ word: string }`\n  - Girilen kelimenin veritabanındaki Türkçe kelime listesinde mevcut olup olmadığını kontrol eder (Geçerli kelime kontrolü).\n- **`POST /api/words/seed`** (Geliştirme / İlk kurulum)\n  - Türkçe 5 harfli kelime listesini SQLite veritabanına toplu aktarır.\n\n---\n\n## 5. Özel Kurallar ve Gereksinimler\n1. **Türkçe Karakter Desteği:** Kelime doğrulama ve aramalarda Türkçe karakterler (`I-ı`, `İ-i`, `Ğ-ğ`, `Ü-ü`, `Ş-ş`, `Ö-ö`, `Ç-ç`) UTF-8 standartlarına uygun işlenmelidir.\n2. **Hata Yönetimi (Error Handling):** Tüm API uç noktalarında standart JSON hata formatı kullanılmalıdır:\n   `{ "success": false, "error": "Hata mesajı" }`\n3. **Veri Doğrulama:** Tüm istek body'leri Zod kullanılarak doğrulanmalı, hatalı veriler HTTP 400 ile reddedilmelidir.",
  "teamleaders": [
    {
      "name": "backend.teamleader",
      "prefix": "backend",
      "mission": "Prisma SQLite mimarisinin kurulması, veri modellerinin ve migration'ların yönetilmesi, liderlik tablosu ile Türkçe kelime API servislerinin geliştirilmesi."
    }
  ]
}