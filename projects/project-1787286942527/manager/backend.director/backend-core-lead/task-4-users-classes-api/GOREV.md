# Atomik Görev: Kullanıcı ve Sınıf REST API Uç Noktaları

Açıklama: GET /api/users (ve mock profil desteği için /api/users/current) ile GET/POST /api/classes rotalarının kodlanması. Veritabanı sorguları için @/lib/prisma kullanılacaktır.
Hedef Dosyalar: ["src/app/api/users/route.ts","src/app/api/classes/route.ts"]