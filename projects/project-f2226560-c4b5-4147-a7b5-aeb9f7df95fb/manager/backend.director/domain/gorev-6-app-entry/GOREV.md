# Atomik Görev: Express Uygulama Yapılandırması

Açıklama: Express instance'ının oluşturulması, `cors`, `express.json()` ve merkezi hata middleware'i (`@/middlewares/errorHandler`) entegrasyonunun yapılması.
Hedef Dosyalar: ["src/app.ts"]