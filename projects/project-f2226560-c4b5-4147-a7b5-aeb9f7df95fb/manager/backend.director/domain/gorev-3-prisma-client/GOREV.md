# Atomik Görev: Prisma İstemci Örneği (Singleton)

Açıklama: Uygulama genelinde kullanılmak üzere `@prisma/client` üzerinden singleton PrismaClient örneğinin oluşturulması ve dışa aktarılması.
Hedef Dosyalar: ["src/lib/prisma.ts"]