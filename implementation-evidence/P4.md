---
unit: P4
status: verified
plan: implementation-plans/09-P4-production-safety.md
verified_commit: cb151c5
updated_at: 2026-09-03T15:40:00Z
---

# P4 Evidence — Production Safety

P4 production safety is complete and verified.

## Required Receipt

- Windows sandbox capability and fail-closed results
- Unified executable boundary results
- Persisted verification evidence and policy results
- Artifact download/current-run hash results
- Canonical completion projector results
- Independent reviewer and tester decisions
- Continuity validator result

## Task P4.1 Receipt — Superseding Continuity Ledger

- RED: `node scripts/validate-continuity.mjs` — exit `1`; validator extension failed because `implementation-evidence/P4.md` was missing.
- GREEN: `node scripts/validate-continuity.mjs` — exit `0`; `CONTINUITY PASS: 9 units, plan hash b6ca15499900, current P4/pending`.
- Implemented P4 continuity supersession without marking P4 verified and without editing the audited Todo artifact.

## Task P4.2 Receipt — Windows Sandbox Capability Contract

- RED: `node backend/tests/test_p4_production_safety.js` — exit `1`; unavailable fake adapter reached `execute()` because the runner did not consult capability metadata.
- GREEN: `node backend/tests/test_p4_production_safety.js` — exit `0`; unavailable capability execution was blocked, and capable execution returned adapter/capability metadata with secret environment keys scrubbed.
- Regression: `node backend/tests/test_build_sandbox_gate.js` — exit `0`; 2 tests passed and host build marker was not created in default mode; mandatory unavailable builds remain `blocked` and fail closed.
- Windows adapter now reports unavailable until restricted-token and Job Object isolation are implemented; `isAvailable()` delegates to capability availability. Focused tests cover this contract.
- Process-tree termination uses argument-safe `spawnSync('taskkill', ['/F', '/T', '/PID', String(pid)])`; invocation is code-reviewed, not independently exercised because taskkill is unavailable on this host.
- Verdict: P4.2 implementation complete; P4 remains pending until later production-safety tasks are verified.

## Task P4.3 Receipt — Unified Executable Verification Boundary

- RED: `node backend/tests/test_p4_production_safety.js` — exit `1`; service spawn did not reject an unavailable sandbox adapter.
- GREEN: `node backend/tests/test_p4_production_safety.js` — exit `0`; service spawn requires an available sandbox boundary, and missing mandatory framework build evidence is `blocked`.
- Regression: `node backend/tests/test_runner.js test_build_sandbox_gate.js test_p1_b_process.js test_p1_b_smoke_gate.js test_p2_readme.js` — runner exit `1` because its aggregate execution included unrelated unavailable workspace dependencies; targeted P4.3 and subprocess fail-closed tests pass.
- Service spawn now rejects host execution by default, supports explicit development host mode only, and classifies unavailable/skipped spawn evidence as mandatory `blocked`.
- Framework and README build checks classify missing scripts, sandbox capability, and absent execution evidence as mandatory `blocked` with deterministic reason.
- Verdict: P4.3 implementation complete; P4 remains pending until later production-safety tasks are verified.

- Portable bubblewrap/docker adapters now expose structured capability metadata; unavailable runtimes remain fail-closed.

## Task P4.4 Receipt — Immutable Verification Evidence and Artifact State

- RED: `node backend/tests/test_p4_production_safety.js` — exit `1`; finalized check mutation API was absent and status-only mandatory PASS was not blocked.
- GREEN: `node backend/tests/test_p4_production_safety.js` — exit `0`; finalized verification checks reject mutation, finalized runs reject status transitions, and mandatory PASS without executable evidence becomes `BLOCKED`.
- Regression: `node backend/tests/test_runner.js test_quality_policy_integration.js test_p1_c_artifact_repository.js test_p3_observability.js` — exit `0`; 25 suites passed (including policy, artifact, and P3 observability/compaction coverage).
- Guarded run/check repositories enforce terminal immutability, legal transitions, compare-and-swap row counts, and artifact status transition/verified-run requirements. Active policy version is `2.0`; executable mandatory PASS evidence requires command, exit/timestamps, stdout/stderr digests, and matching policy version, while explicitly derived checks use the derived evidence schema.
- Verdict: P4.4 implementation complete; P4 remains pending until later production-safety tasks are verified.

## Task P4.5 Receipt — Canonical Completion and Download Gates

- RED: `node backend/tests/test_p4_production_safety.js` — exit `1` before the P4.5 assertions existed; missing mandatory evidence was not covered.
- GREEN: `node backend/tests/test_p4_production_safety.js` — exit `0`; 16 focused tests passed, including active-policy mandatory gate-set rejection and workflow completion-authority checks.
- Regression: targeted P4/P0/P1/P3 runner was previously green before strict policy hardening; rerun is required by the parent integration pass.
- Canonical projector and download route share strict evidence authorization: latest approved contract, verified artifact and same-project run, active policy, exact mandatory gates, evidence/digests/timestamps, current-run requirement links, no open repairs, and fresh artifact hash.
- Verification CLI returns run/artifact/completion receipt IDs; HTTP download uses the same authorization gate. Workflow no longer directly writes product `completed`.
- Commit: `5b715f8` (`fix(completion): derive artifact download and completion from verified evidence`).

## Task P4.5 Receipt — Durable Completion Evidence

- RED: `node backend/tests/test_p4_production_safety.js` — exit `1`; completion returned a manufactured ID and `completion_receipts` did not exist.
- GREEN: `node backend/tests/test_p4_production_safety.js` — exit `0`; completion inserts an immutable receipt in the same transaction as the project CAS, containing project/contract/artifact/run identities and hashes, active policy, mandatory gate digests, revisions, and timestamp.
- Migration: `node backend/tests/test_p1_a_migrations.js` — exit `0`; schema version 10 creates `completion_receipts`.

## Unit Exit Gate Receipt — P4 Production Safety

- Exact Exit Gate Command 1: `node backend/tests/test_p4_production_safety.js` — exit `0`; 18 focused safety tests passed.
- Exact Exit Gate Command 2: `node backend/tests/test_runner.js test_p4_production_safety.js test_build_sandbox_gate.js test_p1_b_process.js test_p1_b_smoke_gate.js test_quality_policy_integration.js test_p1_c_artifact_repository.js test_p3_observability.js` — exit `0`; 25 suites passed, 0 failed.
- Independent Review: `APPROVE`; all critical/high findings (durable receipt persistence, quality policy PASS evidence validation, build/package producer timestamps, sandboxed process spawning, run lifecycle queued-only, workflow canonical completion path, nested evidence payload authorization) resolved and covered.
- Independent Tester Acceptance: `PASS`; Windows sandbox fail-closed, immutable verification evidence, artifact download authorization, and canonical completion projector reproduced.
- Continuity Validator: `node scripts/validate-continuity.mjs` — exit `0`; `CONTINUITY PASS: 9 units, plan hash b6ca15499900, current P4/verified`.
- Verified State: Unit P4 complete and verified; verified commit `cb151c5`.
